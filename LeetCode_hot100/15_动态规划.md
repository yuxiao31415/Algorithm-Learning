## 81.爬楼梯

```java
class Solution {
    public int climbStairs(int n) {
        int[] f = new int[n+1];
        f[0]=f[1]=1;
        for(int i =2;i<=n;i++){
            f[i] = f[i-1]+f[i-2];
        }
        return f[n];

    }
}
```
### 位置

[70. 爬楼梯 - 力扣（LeetCode）](https://leetcode.cn/problems/climbing-stairs/description/?envType=study-plan-v2&envId=top-100-liked)

## 82.杨辉三角**  
```java
class Solution {
    public List<List<Integer>> generate(int numRows) {
        List<List<Integer>> ans = new ArrayList<>();
        int[] row1 = new int[]{1};
        int[] row2 = new int[]{1,1};
        ans.add(Arrays.stream(row1).boxed().toList());
        if(numRows==1) return ans;
        ans.add(Arrays.stream(row2).boxed().toList());
        for(int i=3;i<=numRows;i++){
            int[] row = new int[i];
            row[0] = row[i-1]=1;
            for(int j=1;j<i-1;j++){
                row[j] = ans.getLast().get(j-1)+ans.getLast().get(j);
            }
            ans.add(Arrays.stream(row).boxed().toList());
        }
        return ans;

    }
}
```
### 总结  
```java
// 最安全通用的 数组->list的方案
Integer[] arr = {1, 2, 3};
List<Integer> list = new ArrayList<>(Arrays.asList(arr));
```
### 位置  
[118. 杨辉三角 - 力扣（LeetCode）](https://leetcode.cn/problems/pascals-triangle/description/?envType=study-plan-v2&envId=top-100-liked)

## 83.打家劫舍  //////////////
```java
class Solution {
    public int rob(int[] nums) {
        int[] f = new int[nums.length+1];// f[i] 偷完i家之后最多有多少钱 f[n]
        f[0] = 0;
        f[1] = nums[0];
        for(int i=1;i<nums.length;i++){
            f[i+1] = Math.max(f[i],f[i-1]+nums[i]);
        }
        return f[nums.length];
    }
}
```
### 思路

```
第i位置的最优解,max(当前位置值+上上个位置的值,上个位置的值)
```

### 位置

[198. 打家劫舍 - 力扣（LeetCode）](https://leetcode.cn/problems/house-robber/description/?envType=study-plan-v2&envId=top-100-liked)

## 84.完全平方数  
## 84.完全平方数  ###########

```java
class Solution {
    public int numSquares(int n) {
        int[] f = new int[n+1];
        Arrays.fill(f,Integer.MAX_VALUE);
        f[0] =0;
        for(int j=1;j<=n;j++){
            for(int i=1;i*i<=j;i++){
                f[j] = Math.min(f[j],f[j-i*i]+1);
            }
        }
        return f[n];
    }
}

```

### 思路

背包问题

### 位置

[279. 完全平方数 - 力扣（LeetCode）](https://leetcode.cn/problems/perfect-squares/description/?envType=study-plan-v2&envId=top-100-liked)

## 85.零钱兑换

```java
class Solution {
    public int coinChange(int[] coins, int amount) {
        int n = coins.length;
        int max = Integer.MAX_VALUE;
        int[] f= new int[amount+1];
        Arrays.fill(f,max);
        f[0]=0;
        for(int i=0;i<n;i++){
            for(int j=coins[i];j<=amount;j++){
                if(f[j-coins[i]]!=max){
                    f[j] = Math.min(f[j-coins[i]]+1,f[j]);
                }
            }
        }
        return f[amount] == max ? -1 : f[amount];
    }
}
```

### 思路

### 位置

[322. 零钱兑换 - 力扣（LeetCode）](https://leetcode.cn/problems/coin-change/description/?envType=study-plan-v2&envId=top-100-liked)

## 86.单词拆分

```java
class Solution {
    public boolean wordBreak(String s, List<String> wordDict) {
        HashSet<String> set = new HashSet<>(wordDict);
        boolean[] valid = new boolean[s.length()+1];// 前i个字母是否可以被拆分
        valid[0] = true;
        for(int i=1;i<=s.length();i++){
            for(int j=0;j<i&&valid[i]==false;j++){
                if(set.contains(s.substring(j,i))&&valid[j]){
                    valid[i] = true;
                }
            }
        }
        return valid[s.length()];
    }
}
```

### 位置

[139. 单词拆分 - 力扣（LeetCode）](https://leetcode.cn/problems/word-break/description/?envType=study-plan-v2&envId=top-100-liked)

## 87.最长递增子序列  
```java
class Solution {
    public int lengthOfLIS(int[] nums) {
      int[] f= new int[nums.length]; //以i结尾 一般是f[n] 前i个 一般是f[n+1];
                                    //f[i] 的含义不是“前 i 个的 LIS”，而是“以第 i 个数结尾的 LIS”,所以最大值不一定是f[n]
      int ans =1;
      Arrays.fill(f,1);
      for(int i=1;i<nums.length;i++){
        for(int j=0;j<i;j++){
            if(nums[i]>nums[j]){
                f[i] = Math.max(f[i],f[j]+1);
            }
            ans = Math.max(f[i],ans);
        }
      } 
      return ans; 
    }
}
```

### 位置

[300. 最长递增子序列 - 力扣（LeetCode）](https://leetcode.cn/problems/longest-increasing-subsequence/description/?envType=study-plan-v2&envId=top-100-liked)

## 88.乘积最大子数组
```
class Solution {
    public int maxProduct(int[] nums) {
        int n = nums.length;
        int[] fmax = new int[n]; // f[i] 以nums[i] 结尾的子数组的最大值
        int[] fmin = new int[n];
        fmax[0] = fmin[0] = nums[0];
        int ans = fmax[0];
        for(int i=1;i<n;i++){
            fmax[i] = Math.max(Math.max(nums[i],nums[i]*fmax[i-1]),nums[i]*fmin[i-1]);
            fmin[i] = Math.min(Math.min(nums[i],nums[i]*fmax[i-1]),nums[i]*fmin[i-1]);
            ans = Math.max(ans,fmax[i]);
        }
        return ans;
    }
}
```
### 思路

```
nums=[2, 3, -2, 4]
fmax=[2, 6, -2, 4]
fmin=[2, 3,-12, -48]
```

### 位置

[152. 乘积最大子数组 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-product-subarray/description/?envType=study-plan-v2&envId=top-100-liked)

## 89.分割等和子集

```java
//DP
class Solution {
    public boolean canPartition(int[] nums) {
        int s = 0;
        for (int num : nums) {
            s += num;
        }
        if (s % 2 != 0) {
            return false;
        }
        s /= 2; // 注意这里把 s 减半了
        int n = nums.length;
        boolean[][] f = new boolean[n + 1][s + 1];// 能否从n个元素中找到和恰好等于s(总和的一半)的子序列,因为总和已经等于2s,如果有子序列和为s说明剩下的一定也为s
        f[0][0] = true;
        for (int i = 0; i < n; i++) {
            int x = nums[i];
            for (int j = 0; j <= s; j++) {
                f[i + 1][j] = j >= x && f[i][j - x] || f[i][j];
            }
        }
        return f[n][s];
    }
}
```
### 位置

[416. 分割等和子集 - 力扣（LeetCode）](https://leetcode.cn/problems/partition-equal-subset-sum/description/?envType=study-plan-v2&envId=top-100-liked)

## 90.最长有效括号  
```java
// 栈
class Solution {
    public int longestValidParentheses(String s) {
        Deque<Integer> stack = new ArrayDeque<>();
        stack.push(-1); // 虚拟断点,表示在字符串开始之前，有一个‘上一次不合法的位置’
        int ans = 0;

        for (int i = 0; i < s.length(); i++) {
            char c = s.charAt(i);

            if (c == '(') {
                stack.push(i);
            } else { // c == ')'
                stack.pop();
                if (stack.isEmpty()) { // 说明断点被你pop了,否则一定会有断点,因为pop了(
                    // 没法匹配，设为新的断点
                    stack.push(i);
                } else {
                    // 计算当前合法长度
                    ans = Math.max(ans, i - stack.peek());
                }
            }
        }
        return ans;
    }
}
```
### 思路

```
全部情况
() 
)
))
)()
(()
()(()
```

### 位置

[32. 最长有效括号 - 力扣（LeetCode）](https://leetcode.cn/problems/longest-valid-parentheses/description/?envType=study-plan-v2&envId=top-100-liked)







